# Greenlight

**Status: pre-alpha, design phase. No code yet.**

Greenlight is an opinionated testing framework for PHP 8.4+: fully typed attribute-driven tests, parallel-first execution with flat memory, a lifecycle-safe mocking library of its own, and a plugin API that gives extensions the real runtime context.

Start with the [Product Requirements Document](docs/PRD.md).
