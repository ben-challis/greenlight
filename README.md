# Greenlight

**Status: pre-alpha — design phase. No code yet.**

Greenlight is a modern, opinionated testing framework for PHP 8.4+: fully typed attribute-driven tests, parallel-first execution with flat memory, a native lifecycle-safe mocking library, and a plugin API that hands extensions the real runtime context.

Start with the [Product Requirements Document](docs/PRD.md).
