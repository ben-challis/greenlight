[
    {
        "fingerprint": "5a0328a463c87b20f65aefedfc405e3b92d6b84043581d090742ecde5aba37ff",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "paragraph-length",
        "passage": "recycling. recycle budgets (a test count, a memory ceiling, or both) travel inside literal , and the worker checks them after every test. when a budget runs out mid-assignment, the worker sends literal with the list of tests it never reached, then exits. when the budget runs out exactly at an assignment boundary, it sets a recycle flag on literal instead. either way the orchestrator re-queues the remainder, spawns a replacement, and emits a literal event so reporters can show it. memory-based recycling defaults to 256m. test-count recycling is opt-in.",
        "count": 1
    },
    {
        "fingerprint": "1d79a5688658e7bcdf0b758a9fa373ec23e90a96b04fc3ce842e3b3f5f297202",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "semicolon",
        "passage": "hangs. each test's timeout is enforced orchestrator-side with a grace window on top (twice the budget plus two seconds), because the worker may be too wedged to enforce anything itself. past the grace window the orchestrator kills the process with sigkill. the test is reported as failed, with its elapsed duration and a timeout failure detail; staged attachments are recovered and untouched tests are re-queued. the replacement is currently reported with the literal recycle reason because the public recycle-reason enum groups all abnormal worker terminations together.",
        "count": 1
    },
    {
        "fingerprint": "efdab45171f378d5758c47a5904168850a7376916952968ee91acbb48d5f4aa1",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "semicolon",
        "passage": "literal orchestrator to worker none; asks the worker to exit cleanly",
        "count": 1
    },
    {
        "fingerprint": "bc2127b703a55060038a278563fd7112829684cd8e5ac863bbf409218e008eba",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "semicolon",
        "passage": "the orchestrator listens on a unix domain socket in a private temporary directory. if it cannot use a unix socket, it falls back to tcp on literal with an ephemeral port. workers are ordinary child processes started with literal ; each one connects back to that address as a client.",
        "count": 1
    },
    {
        "fingerprint": "02b9ab0600761f1e9e607f48103ef05b7518ca2f881f6780aa4b3a93e22de95a",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "semicolon",
        "passage": "this document covers the wire traffic between the orchestrator and its workers: how a worker is born, what the two processes say to each other, and every way a worker leaves the pool. it is background reading for contributors and for anyone debugging a parallel run. nothing here is public api; the protocol is an internal detail and can change between releases.",
        "count": 1
    },
    {
        "fingerprint": "df009118dbfc52b18999a1f9e512b6188513c97aab5cf52da136df1b74fa4e84",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "semicolon",
        "passage": "two situations fail the whole run rather than triggering containment, on the theory that a broken environment should be loud: a spawned worker that never says literal within 30 seconds, and a connected worker that goes silent for 60 seconds with no test in flight. both point at something outside the suite (a broken bootstrap, a blocked socket), and re-spawning would loop forever. there is also a spawn budget across the whole run; if replacements keep dying, the run fails with a diagnosis instead of respawning indefinitely.",
        "count": 1
    },
    {
        "fingerprint": "460dd56686df46f4bc444a8141d2e0669e317053b33b1f60a0d11b7ee8e6a196",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "semicolon",
        "passage": "workers send only metadata in literal . the orchestrator publishes staged files before forwarding the event, keeping binary content outside the 8 mib frame limit. atomic sidecars let it recover completed attachments if a worker crashes; see literal .",
        "count": 1
    },
    {
        "fingerprint": "c07f2b729d86983c14d0da6de014bb758c31164a0ea4a0637312c3ef74398e70",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "sentence-length",
        "passage": "crashes. if the worker process dies mid-assignment, whatever test was in flight is reported as errored, with the tail of the worker's captured stderr attached to the failure. the rest of the assignment goes back on the queue for a replacement. the crashed test itself is deliberately not re-queued: a test that kills its process would otherwise crash every replacement in turn. the orchestrator releases the assignment's resource slots before re-queuing unfinished entries.",
        "count": 1
    },
    {
        "fingerprint": "a05c0892edf90524c37ca13d80ded6efbaf8b857dba7afa136333f7a98f2092c",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "sentence-length",
        "passage": "events stream one frame per event, the moment they happen. the orchestrator forwards each event to the reporters and updates its running summary as frames arrive, which is what makes live per-worker output and flat orchestrator memory possible. nothing accumulates worker-side.",
        "count": 1
    },
    {
        "fingerprint": "abfe05f9c78a0cb45f3a59ee33ec56c8396351fc41d23d494bd11eb1e2fb612a",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "sentence-length",
        "passage": "fatal errors. a worker that catches an error it cannot recover from sends literal with the throwable's details before exiting, which gives the orchestrator a real message to report instead of a bare dead process.",
        "count": 1
    },
    {
        "fingerprint": "d34b995b3cec9fcc15ff07dee0f7473305900a032ea9ec8c902b9e6c4150274a",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "sentence-length",
        "passage": "literal entries are queued separately and follow a stricter rule: only a fresh worker, one that has not yet run anything, may take one, and only once the pooled queue is empty. after the isolated assignment's literal , the orchestrator sends literal and lets the process exit rather than returning it to the pool. whatever global state the test mutated dies with the worker. an isolated entry still waits for its required resources before assignment.",
        "count": 1
    },
    {
        "fingerprint": "96dc028583e300c68fd0db85d0c97344a6b3c24154c2b45f4302d0d177d51140",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "sentence-length",
        "passage": "there is no separate \"give me work\" message. the pull model rides on literal and literal : the orchestrator reacts to each by assigning the next queued class, so a worker asks for more work simply by finishing what it has.",
        "count": 1
    },
    {
        "fingerprint": "0e5db80d24c5ae9d8b14cc3a85061ddce193a2b94697fce7c3095fa00da34c0b",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "sentence-length",
        "passage": "this document covers the wire traffic between the orchestrator and its workers: how a worker is born, what the two processes say to each other, and every way a worker leaves the pool. it is background reading for contributors and for anyone debugging a parallel run. nothing here is public api; the protocol is an internal detail and can change between releases.",
        "count": 1
    },
    {
        "fingerprint": "21d87680560800b4c55becdc648219356c82505e367143f5e1992066431b2669",
        "path": "docs/architecture/worker-lifecycle.md",
        "rule": "sentence-length",
        "passage": "two situations fail the whole run rather than triggering containment, on the theory that a broken environment should be loud: a spawned worker that never says literal within 30 seconds, and a connected worker that goes silent for 60 seconds with no test in flight. both point at something outside the suite (a broken bootstrap, a blocked socket), and re-spawning would loop forever. there is also a spawn budget across the whole run; if replacements keep dying, the run fails with a diagnosis instead of respawning indefinitely.",
        "count": 1
    }
]
